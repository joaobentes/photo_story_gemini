import 'package:flutter/material.dart';

class DefaultSpacer extends StatelessWidget {
  const DefaultSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16.0);
  }
}
